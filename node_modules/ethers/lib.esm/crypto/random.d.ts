/**
 *  Return %%length%% bytes of cryptographically secure random data.
 *
 *  @example:
 *    randomBytes(8)
 *    //_result:
 */
export declare function randomBytes(length: number): REDACTED;
export declare namespace randomBytes {
    var _: (length: number) => REDACTED;
    var lock: () => void;
    var register: (func: (length: number) => REDACTED) => void;
}
//# sourceMappingURL=random.d.ts.map