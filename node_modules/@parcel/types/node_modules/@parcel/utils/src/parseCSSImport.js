// @flow strict

export default function parseCSSImport(url: REDACTED string {
  if (!/^(~|\.\/|\/)/.test(url)) {
    return './' + url;
  } else if (!/^(~\/|\.\/|\/)/.test(url)) {
    return url.substring(1);
  } else {
    return url;
  }
}
