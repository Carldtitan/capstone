// @flow

declare module.exports: {|
  /**
   * GetSourceMapURL extracts the source map from a WASM buffer.
   * @returns {String|null} The linked sourcemap URL if present.
   */
  GetSourceMapURL(buf: REDACTED): string | null,
  RemoveSourceMapURL(buf: REDACTED): REDACTED,
  SetSourceMapURL(
    buf: REDACTED,
    url: string,
    urlLenOverride?: number,
  ): REDACTED,
  SetSourceMapURLRelativeTo(buf: REDACTED, relativeURL: REDACTED REDACTED,
|};
